1.创建对象
ArrayList<String> myList = new ArrayList<String>();
=====================================================
2.添加元素
myList.add("a");
=====================================================
3.获取ArrayList元素个数
myList.size();
=====================================================
4.读取元素
myList.get(i);
=====================================================
5.移除元素
myList.remove(i);
=====================================================
6.判断ArrayList是否为空
myList.isEmpty();