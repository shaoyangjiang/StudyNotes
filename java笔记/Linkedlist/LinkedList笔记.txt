1.创建对象
List<String> myList = new LinkedList<String>();
=====================================================
2.添加元素
myList.add("a");
=====================================================
3.获取LinkedList元素个数
myList.size();
=====================================================
4.读取元素
myList.get(i);
=====================================================
5.移除元素
myList.remove(i);
=====================================================
6.判断LinkedList是否为空
myList.isEmpty();